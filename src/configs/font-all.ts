/**
 * @packageDocumentation
 * Shareable Stylelint config: font-all.
 */

import { fontPluginConfigs, type FontShareableConfig } from "../plugin.js";

/** Shareable config that enables the full public font plugin catalog. */
const fontAllConfig: FontShareableConfig = fontPluginConfigs["font-all"];

export default fontAllConfig;
