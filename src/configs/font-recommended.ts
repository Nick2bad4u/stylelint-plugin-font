/**
 * @packageDocumentation
 * Shareable Stylelint config: font-recommended.
 */

import { fontPluginConfigs, type FontShareableConfig } from "../plugin.js";

/** Shareable config that enables the recommended font plugin rules. */
const fontRecommendedConfig: FontShareableConfig =
    fontPluginConfigs["font-recommended"];

export default fontRecommendedConfig;
